apiVersion: v1
kind: Service
metadata:
  name: {{SERVICE_NAME}}
  namespace: dev-platform
spec:
  type: NodePort
  selector:
    app: {{ENV_NAME}}
  ports:
    - port: 80
      targetPort: {{PORT}}
      nodePort: {{NODE_PORT}}
